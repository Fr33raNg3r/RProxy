# 强制所有文本文件在任何平台检出时都使用 LF 换行。
#
# 为什么需要：Windows 上 git 的 core.autocrlf=true 会在检出时把 LF 转成 CRLF。
# 如果在 Windows 本地打包 release 产物，CRLF 会被原样打进 tar 包，在 Debian 上
# 造成 `#!/bin/bash^M` 找不到解释器，脚本静默失败。1.3.0 就因此让
# load-cn-ips.sh 无法执行 → cn_ips 集合为空 → 国内流量误走代理变慢。
#
# 本文件提交在仓库里，对所有人、所有平台、所有检出方式生效，
# 不依赖各自机器上的 core.autocrlf 配置。
* text=auto eol=lf

# 二进制文件不做任何换行符转换（当前仓库没有被跟踪的二进制文件，这里是预防性声明）
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.woff  binary
*.woff2 binary
*.ttf   binary
*.dat   binary
*.zip   binary
*.gz    binary
*.pdf   binary
